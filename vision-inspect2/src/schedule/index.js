/**
 * main schedule starters
 * @author duanj
 */

/*const {
    startPlanSchedule,
    startCaptureSchedule,
    startSubmitEventsSchedule,
} = require('./inspect');*/

const startSchedules = () => {
    // startPlanSchedule();
    // startCaptureSchedule();
    // startSubmitEventsSchedule();
};

module.exports = startSchedules;